import { startExecute } from "./engine.mjs";
import { getCurrentSchemaValue, getCurrentInstanceValue } from "./helper.mjs";
export const engine = {
    startExecute,
};
export const helper = {
    getCurrentSchemaValue,
    getCurrentInstanceValue,
};
